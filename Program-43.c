// WAP that will read a line and replace all spaces with "0x20".
#include <stdio.h>
#include <string.h>
int main()
{
    char str[100];
    int i, len;
    printf("\nenter the string: ");
    scanf("%s", &str);
    len = strlen(str);
    for (i = 0; i < len; i++)
    {
        if (str[i] == ' ')
        {
            
        }
    }
    return 0;
}